# Code tips

Use `git_status` / `git_diff` / `git_log` before patching.
Keep patches unique (`old` must match once unless replace_all).
Create folders with `create_directory`; find sources with `find_files`.
Run tests via `run_command` with argv form.
