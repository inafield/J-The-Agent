# Web tips

DuckDuckGo works without a key but may be less reliable; Brave/Tavily/SearXNG are usually better when configured.
Use `web_search` for current facts; `fetch_url` for a specific page; `open_url` to show the user.
Fetching/opening asks the user first. Local/private hosts are blocked unless allowed via `ja allow-local PORT`.
