# Reminders tips

when= accepts English and Russian: `in 2 hours` / `через 2 часа`, `at 07:00` / `в 07:00`,
`every day at 07:00` / `каждый день в 07:00`, or ISO datetime.
Schedules use launchd (macOS) or cron (Linux). Overdue items appear at Companion start and login check.
