# Memory tips

Write durable facts only. Prefer `memory_append` for new notes; use `memory_write` for full rewrites.
Update `soul.md` when the user asks for a lasting style change.
Do not dump chat logs into memory.
